[metadata]
name = "ghostctl"
description = "Search ghostctl commands and resources (placeholder)"
requirements = ["echo"]

[source]
command = '''echo -e "ghostctl publish\nghostctl preview\nghostctl themes\nghostctl posts\nghostctl pages\nghostctl members\nghostctl settings\nghostctl deploy"'''

[preview]
command = "echo 'ghostctl is not yet implemented — this is a placeholder channel for when @ghostctl/ ships'"
